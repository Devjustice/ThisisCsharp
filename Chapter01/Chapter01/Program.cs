using System;
using static System.Console;
namespace Chapter01
{
    class Program
    {
        //프로그램이 실행되는 곳
        /*
         static void Main(string[] args)
         {

             if (args.Length == 0)
             {
                 Console.WriteLine("Hello World!");
                 return;
             }

             WriteLine("Hello, {0}!", args[0]);
         }
 */
    }

    //static (한정자), void (반환형식) main(매소드), (string[] args) 매게변수
    //static은 프로그램 구동시점 부터 메모리에 할당된다
    //String[] args in Java is an array of strings which stores arguments(인수) passed by command line
    //while starting a program.All the command line arguments are stored in that array.

}
